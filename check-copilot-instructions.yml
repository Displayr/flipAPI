name: Check Copilot Instructions Sync

on:
  pull_request:
    branches: '*'

jobs:
  check-copilot-sync:
    uses: Displayr/flipU/.github/workflows/check-copilot-instructions.yml@hackathon-reusable-workflow
